# Telegram (required)
TELEGRAM_BOT_TOKEN=

# Lark Suite (required)
LARK_APP_ID=
LARK_APP_SECRET=

# OpenAI (required)
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-5.4
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Cohere (required, reranking)
COHERE_API_KEY=

# Qdrant
# Để mặc định http://qdrant:6333 — Docker compose tự override URL này về tên
# service trong network. Dev local trên host: đổi sang http://localhost:6333.
QDRANT_URL=http://qdrant:6333

# App
DB_PATH=data/history.db
TIMEZONE=Asia/Ho_Chi_Minh
RECENT_MESSAGES=8
RAG_MESSAGES=5

# Zalo channel (optional, single-account demo)
# 1. cd src/channels/zalo_bridge && npm install && node login.js   ← scan QR once
# 2. set ZALO_ENABLED=true. session.json next to bridge.js is auto-loaded.
# 3. (Docker) copy session.json into ./data/zalo/ and override ZALO_SESSION_PATH.
ZALO_ENABLED=false
ZALO_NODE_PATH=node
ZALO_SESSION_PATH=
# DM phải chứa cụm này thì sếp mới được forward khi chưa onboarding xong.
# Sau khi onboard rồi thì chỉ DM từ boss / group đã đăng ký mới qua agent.
ZALO_ONBOARD_PHRASE=khởi tạo trợ lý
