{
  "name": "zalo-bridge-probe",
  "version": "0.1.0",
  "description": "Probe — verify zca-js QR login + listen + send work end-to-end before building the Python RPC bridge.",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "login": "node login.js",
    "listen": "node listen.js",
    "send": "node send.js"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "zca-js": "^2.0.0-beta.27"
  }
}
